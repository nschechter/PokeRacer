import React, { Component } from 'react'
import { connect } from 'react-redux'

class RaceList extends React {

}

const mapStateToProps = (state) => {
  return  (
  account: state.Account
  races: state.RaceList
  )
}
